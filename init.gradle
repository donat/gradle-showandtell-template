<#GradleTemplate>
file=init.gradle<#if dsl =='kotlin'>.kts</#if>
skip=<#if dsl =='kotlin'>true</#if>
</#GradleTemplate>

<#if gradleVersion.sameOrNewer('7.5')>
// A newer Gradle version (${gradleVersion.version}) is used.
<#else>
// An older Gradle version (${gradleVersion.version}) is used.
</#if>


// an init script
